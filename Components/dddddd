// 'use client'
// import React, { useEffect, useRef, useState } from 'react';
// import { AnimatePresence } from 'framer-motion';
// import AdditionalContent from './AdditionalContent';
// import ProjectsProp from './ProjectsProp';

// const Projects = () => {
//     const [scrolling, setScrolling] = useState({ top: 0, bottom: 0 });
//     const RefTarget = useRef<HTMLDivElement>(null);

//     useEffect(() => {
//         const handleScroll = () => {
//             if (RefTarget.current) {
//                 const rect = RefTarget.current.getBoundingClientRect();
//                 const currentTopPosition = rect.top;
//                 const currentBottomPosition = rect.bottom - window.innerHeight;
//                 setScrolling({ top: currentTopPosition, bottom: currentBottomPosition });
//             }
//         };

//         window.addEventListener('scroll', handleScroll);

//         // Clean up the event listener when the component unmounts
//         return () => {
//             window.removeEventListener('scroll', handleScroll);
//         };
//     }, []);

//     return (
//         <div className='h-full' ref={RefTarget}>
//             <div className='grid grid-cols-2 gap-10'>
//                 <div className='flex-col flex gap-5'>
//                     <ProjectsProp className='h-[700px] w-[400px] bg-orange-500 sticky top-0 my-20'>
//                         box-1
//                     </ProjectsProp>
//                     <ProjectsProp className='h-[700px] w-[400px] bg-blue-500 sticky top-10 my-20'>
//                         box-2
//                     </ProjectsProp>
//                     <ProjectsProp className='h-[700px] w-[400px] bg-green-500 sticky top-20 my-20'>
//                         box-3
//                     </ProjectsProp>
//                     <ProjectsProp className='h-[700px] w-[400px] bg-yellow-500 sticky top-32 my-20'>
//                         box-4
//                     </ProjectsProp>
//                 </div>
//                 <div className='bg-red-600 sticky top-36 w-[500px] h-[400px] my-20'>
//                     <AnimatePresence>
//                         <AdditionalContent top={scrolling.top} bottom={scrolling.bottom} />
//                     </AnimatePresence>
//                 </div>
//             </div>
//             <div className='fixed top-0 left-0'>{scrolling.top}</div>
//             <div className='fixed top-0 right-32'>{scrolling.bottom}</div>
//         </div>
//     );
// }

// export default Projects;

// import React, { useRef } from 'react';
// import Image from 'next/image';
// import { motion, useMotionValueEvent, useScroll } from 'framer-motion';
// import b1o from "../public/download.jpeg";
// import b2o from "../public/usgs-ZpC7sJVovLM-unsplash.jpg";
// import b3o from "../public/usgs-Jd09hiCUPCs-unsplash.jpg";

// interface AdditionalContentProps {
//     top: number;
//     bottom: number;
// }

// const AdditionalContent: React.FC<AdditionalContentProps> = ({ top, bottom }) => {
//     const element = useRef(null)
//     const { scrollYProgress } = useScroll({
//         target: element,
//         offset: ["end start", "start end"]
//     })

// useMotionValueEvent(scrollYProgress, "change", (latest) => {
//   console.log("Page scroll: ", latest)
// })

    //     if (top < 0 && bottom > 2300) {
    //         return (
    //             <motion.div
    //                 initial={{ scale: 0, rotate: 0 }}
    //                 animate={{ rotate: 90, scale: 1 }}
    //                 exit={{ opacity: 0, rotate: 0, scale: 0 }}
    //                 transition={{
    //                     type: "spring",
    //                     duration: 1,
    //                     stiffness: 260,
    //                     damping: 20
    //                 }}
    //             >
    //                 <Image src={b1o} alt='box1' />
    //             </motion.div>
    //         );
    //     } else if (top < 0 && bottom > 1500) {
    //         return (
    //             <motion.div
    //                 initial={{ translateY: 0, scale: 0 }}
    //                 animate={{ translateY: -90, scale: 1 }}
    //                 exit={{ opacity: 0, translateY: 0, scale: 0 }}
    //                 transition={{
    //                     type: "spring",
    //                     stiffness: 260,
    //                     damping: 20,
    //                     duration: 1,
    //                 }}
    //             >
    //                 <Image src={b2o} alt='box2' width={400} />
    //             </motion.div>
    //         );
    //     } else if (top < 0 && bottom > 800) {
    //         return (
    //             <motion.div
    //                 initial={{ translate: 0 }}
    //                 animate={{ translateY: -90, scale: 1 }}
    //                 exit={{ opacity: 0, translateY: 0, scale: 0 }}
    //                 transition={{
    //                     duration: 1,
    //                     type: "spring",
    //                     stiffness: 260,
    //                     damping: 20
    //                 }}
    //             >
    //                 <Image src={b3o} alt='box3' width={400} />
    //             </motion.div>
    //         );
    //     }
    //     return null; // Return null if none of the conditions match
    // }

// export default AdditionalContent;
